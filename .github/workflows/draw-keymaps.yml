name: Draw ZMK keymaps

on:
  workflow_dispatch:
  push:
    paths:
      - "config/keymap.keymap"
      - "keymap_drawer.config.yaml"

jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    permissions:
      contents: write
    with:
      keymap_patterns: "config/keymap.keymap"
      output_folder: "keymap-drawer"
